# Railway — Neon DB + Docker backend (repo root, no custom build/start commands).
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/server/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
